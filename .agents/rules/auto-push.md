# Git Auto-Push Rule

Whenever any changes are made to files in this repository:
1. Stage the modified/added/deleted files using `git add .`
2. Commit the changes with a concise and descriptive message using `git commit -m "..."`
3. Push the changes to the GitHub remote repository using `git push origin main`
