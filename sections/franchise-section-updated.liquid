<style>
.vs-section-{{ section.id }} {
  background: linear-gradient(180deg, {{ section.settings.bg_color_top }} 0%, {{ section.settings.bg_color_mid }} 48%, {{ section.settings.bg_color_bottom }} 100%);
  padding: 60px 18px 40px;
  overflow: hidden;
  position: relative;
  color: #111;
  box-sizing: border-box;
}
.vs-section-{{ section.id }} * {
  box-sizing: border-box;
}
.vs-section-{{ section.id }}:before {
  content: "";
  position: absolute;
  top: -120px;
  right: -120px;
  width: 380px;
  height: 380px;
  border-radius: 50%;
  background: radial-gradient(circle, rgba(255,170,0,.16), transparent 70%);
  filter: blur(14px);
}
.vs-section-{{ section.id }} .container {
  max-width: 1180px;
  margin: auto;
  position: relative;
  z-index: 2;
}

/* HEADER */
.vs-section-{{ section.id }} .heading {
  text-align: center;
  margin-bottom: 50px;
  position: relative;
}
.vs-section-{{ section.id }} .heading h1 {
  margin: 0;
  text-transform: uppercase;
  line-height: .96;
  position: relative;
}
.vs-section-{{ section.id }} .heading .top,
.vs-section-{{ section.id }} .heading .bottom {
  display: block;
  font-weight: 900;
  white-space: nowrap;
  background: linear-gradient(90deg, #ff9200, #ffbc00, #d5a100);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  filter: drop-shadow(0 10px 18px rgba(255,170,0,.18));
  animation: floatText 4s ease-in-out infinite;
}
.vs-section-{{ section.id }} .heading .top {
  font-size: 12.5vw;
  letter-spacing: -1.5px;
}
.vs-section-{{ section.id }} .heading .bottom {
  font-size: 10.2vw;
  margin-top: 4px;
  letter-spacing: -1px;
  animation-delay: .3s;
}
.vs-section-{{ section.id }} .heading h1:after {
  content: "";
  position: absolute;
  left: -30%;
  top: 0;
  width: 28%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255,255,255,.55), transparent);
  transform: skewX(-20deg);
  animation: shine 5s linear infinite;
}
.vs-section-{{ section.id }} .heading-glow {
  width: 160px;
  height: 6px;
  border-radius: 999px;
  margin: 16px auto 0;
  background: linear-gradient(90deg, #ff9d00, #ffc400, #1eb759);
  box-shadow: 0 0 18px rgba(255,170,0,.35);
  animation: pulse 2.5s ease-in-out infinite;
}
.vs-section-{{ section.id }} .heading p {
  margin: 16px auto 0;
  color: #555;
  font-size: 15px;
  line-height: 1.7;
  max-width: 480px;
  font-weight: 500;
}

/* EFFECTS */
@keyframes floatText {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-3px); }
}
@keyframes shine {
  0% { left: -30%; }
  100% { left: 130%; }
}
@keyframes pulse {
  0%, 100% { transform: scaleX(1); opacity: .9; }
  50% { transform: scaleX(1.08); opacity: 1; }
}

/* TIMELINE LUCRATIVE CARDS */
.vs-section-{{ section.id }} .timeline {
  position: relative;
  max-width: 1050px;
  margin: auto;
}
.vs-section-{{ section.id }} .timeline-line {
  position: absolute;
  left: 30px;
  top: 35px;
  width: 4px;
  border-radius: 40px;
  background: linear-gradient(to bottom, #ff9d00, #ffc500, #1eb759);
  box-shadow: 0 0 10px rgba(255, 157, 0, 0.4);
}
.vs-section-{{ section.id }} .item {
  display: flex;
  gap: 20px;
  align-items: flex-start;
  margin-bottom: 30px;
}
.vs-section-{{ section.id }} .item:last-child {
  margin-bottom: 0;
}
.vs-section-{{ section.id }} .icon-wrap {
  position: relative;
  z-index: 3;
}
.vs-section-{{ section.id }} .icon {
  width: 64px;
  min-width: 64px;
  height: 64px;
  border-radius: 50%;
  background: #ffffff;
  border: 3px solid #ff9800;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 10px 25px rgba(255,152,0,.25);
  z-index: 2;
  transition: all 0.3s ease;
}
.vs-section-{{ section.id }} .item:hover .icon {
  transform: scale(1.1) rotate(5deg);
  border-color: #1eb759;
  box-shadow: 0 12px 30px rgba(30,183,89,.35);
}
.vs-section-{{ section.id }} .icon svg, 
.vs-section-{{ section.id }} .icon img {
  width: 30px;
  height: 30px;
  object-fit: contain;
}
.vs-section-{{ section.id }} .icon svg {
  fill: #ff9800;
  transition: fill 0.3s ease;
}
.vs-section-{{ section.id }} .item:hover .icon svg {
  fill: #1eb759;
}

/* LUCRATIVE CARD BODY */
.vs-section-{{ section.id }} .content-card {
  flex: 1;
  background: #ffffff;
  border: 1px solid rgba(255, 170, 0, 0.22);
  border-radius: 18px;
  padding: 24px 28px;
  box-shadow: 0 10px 30px rgba(236, 92, 10, 0.05);
  position: relative;
  transition: all 0.35s cubic-bezier(0.175, 0.885, 0.32, 1.275);
}
.vs-section-{{ section.id }} .content-card:hover {
  transform: translateY(-5px);
  border-color: #ff9d00;
  box-shadow: 0 18px 40px rgba(255, 157, 0, 0.15);
}
.vs-section-{{ section.id }} .step-badge {
  display: inline-block;
  font-size: 11px;
  font-weight: 800;
  letter-spacing: 1px;
  text-transform: uppercase;
  color: #ff9200;
  background: rgba(255, 146, 0, 0.1);
  border: 1px solid rgba(255, 146, 0, 0.25);
  padding: 3px 10px;
  border-radius: 20px;
  margin-bottom: 10px;
}
.vs-section-{{ section.id }} .content h2 {
  margin: 0 0 10px;
  font-size: 22px;
  line-height: 1.2;
  font-weight: 900;
  color: #111111;
  letter-spacing: -0.4px;
}
.vs-section-{{ section.id }} .content h2 span {
  display: block;
  white-space: normal;
}
.vs-section-{{ section.id }} .content p {
  margin: 0;
  color: #555555;
  font-size: 15px;
  line-height: 1.7;
}

/* GRID HEADING STYLES */
.vs-grid-title-{{ section.id }} {
  text-align: center;
  margin-top: 65px;
  margin-bottom: 25px;
}
.vs-grid-title-{{ section.id }} h2 {
  margin: 0;
  font-size: 28px;
  font-weight: 900;
  letter-spacing: -0.5px;
  text-transform: uppercase;
  display: inline-block;
  background: linear-gradient(90deg, #ff9200, #ffbc00, #d5a100);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  filter: drop-shadow(0 8px 15px rgba(255,170,0,.15));
}

/* IMAGE GRID STYLES */
.vs-usage-grid-{{ section.id }} {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 24px;
  perspective: 1500px;
  margin-top: 20px; 
  max-width: 1050px;
  margin-left: auto;
  margin-right: auto;
}
.vs-usage-card-{{ section.id }} {
  position: relative;
  border-radius: 20px;
  overflow: hidden;
  aspect-ratio: 4 / 3;
  box-shadow: 0 15px 35px rgba(0,0,0,0.1);
  transform-style: preserve-3d;
  border: 1px solid rgba(255, 170, 0, 0.2);
  transition: all 0.4s ease;
}
.vs-usage-card-{{ section.id }} img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform {{ section.settings.hover_speed }}s ease;
}
.vs-usage-overlay-{{ section.id }} {
  position: absolute;
  bottom: 0;
  left: 0;
  width: 100%;
  padding: 35px 20px 20px;
  background: linear-gradient(to top, rgba(15,23,42,0.92) 0%, rgba(15,23,42,0.4) 60%, transparent 100%);
  color: #ffffff;
  text-align: left;
  font-weight: 800;
  font-size: 1.15rem;
  letter-spacing: 0.5px;
  text-transform: uppercase;
  transform: translateZ(20px);
  transition: color 0.3s ease;
  display: flex;
  justify-content: space-between;
  align-items: center;
}
.vs-usage-overlay-{{ section.id }} .badge-tag {
  font-size: 10px;
  background: #ff9800;
  color: #fff;
  padding: 4px 8px;
  border-radius: 12px;
  font-weight: 700;
}
.vs-usage-card-{{ section.id }}:hover img { transform: scale(1.08); }
.vs-usage-card-{{ section.id }}:hover { border-color: #ff9d00; box-shadow: 0 20px 45px rgba(255, 157, 0, 0.25); }
.vs-usage-card-{{ section.id }}:hover .vs-usage-overlay-{{ section.id }} { color: {{ section.settings.overlay_hover_color }}; }

/* DESKTOP */
@media(min-width: 769px) {
  .vs-section-{{ section.id }} { padding: 90px 26px 60px; }
  .vs-section-{{ section.id }} .heading { margin-bottom: 70px; }
  .vs-section-{{ section.id }} .heading .top { font-size: 118px; letter-spacing: -4px; }
  .vs-section-{{ section.id }} .heading .bottom { font-size: 92px; letter-spacing: -3px; }
  .vs-section-{{ section.id }} .heading p { font-size: 20px; max-width: 760px; }
  .vs-section-{{ section.id }} .heading-glow { width: 240px; height: 8px; }
  .vs-section-{{ section.id }} .timeline-line { left: 44px; width: 5px; top: 46px; }
  .vs-section-{{ section.id }} .item { gap: 30px; margin-bottom: 40px; }
  .vs-section-{{ section.id }} .icon { width: 90px; min-width: 90px; height: 90px; }
  .vs-section-{{ section.id }} .icon svg, .vs-section-{{ section.id }} .icon img { width: 42px; height: 42px; }
  .vs-section-{{ section.id }} .content h2 { font-size: 26px; line-height: 1.2; }
  .vs-section-{{ section.id }} .content p { font-size: 17px; line-height: 1.8; }
  .vs-grid-title-{{ section.id }} h2 { font-size: 38px; }
}

@media(max-width: 768px) {
  .vs-grid-title-{{ section.id }} { margin-top: 45px; margin-bottom: 15px; }
  .vs-grid-title-{{ section.id }} h2 { font-size: 24px; }
  .vs-usage-grid-{{ section.id }} { gap: 14px; margin-top: 15px; grid-template-columns: repeat(2, 1fr); }
  .vs-usage-card-{{ section.id }} { border-radius: 14px; aspect-ratio: 1 / 1; }
  .vs-usage-overlay-{{ section.id }} { padding: 16px 12px 12px; font-size: 0.85rem; flex-direction: column; align-items: flex-start; gap: 4px; }
}
</style>

<section class="vs-section-{{ section.id }}">
  <div class="container">
    
    <div class="heading">
      <h1>
        {%- if section.settings.heading_top != blank -%}
          <span class="top">{{ section.settings.heading_top }}</span>
        {%- endif -%}
        {%- if section.settings.heading_bottom != blank -%}
          <span class="bottom">{{ section.settings.heading_bottom }}</span>
        {%- endif -%}
      </h1>
      <div class="heading-glow"></div>
      {%- if section.settings.heading_sub != blank -%}
        <p>{{ section.settings.heading_sub }}</p>
      {%- endif -%}
    </div>

    <div class="timeline">
      <div class="timeline-line"></div>

      {%- for block in section.blocks -%}
        <div class="item" {{ block.shopify_attributes }}>
          <div class="icon-wrap">
            <div class="icon">
              {%- if block.settings.custom_icon_svg != blank -%}
                {{ block.settings.custom_icon_svg }}
              {%- elsif block.settings.custom_icon_image != blank -%}
                {{ block.settings.custom_icon_image | image_url: width: 100 | image_tag: loading: 'lazy' }}
              {%- else -%}
                <svg viewBox="0 0 24 24"><path d="M12 2L3 7v10l9 5 9-5V7l-9-5z"/></svg>
              {%- endif -%}
            </div>
          </div>

          <div class="content-card">
            <span class="step-badge">Feature 0{{ forloop.index }}</span>
            <div class="content">
              {%- if block.settings.title != blank -%}
                <h2>{{ block.settings.title }}</h2>
              {%- endif -%}
              {%- if block.settings.text != blank -%}
                <p>{{ block.settings.text }}</p>
              {%- endif -%}
            </div>
          </div>
        </div>
      {%- endfor -%}
    </div>

    <!-- Editable Grid Title Area -->
    {%- if section.settings.grid_heading != blank -%}
      <div class="vs-grid-title-{{ section.id }}">
        <h2>{{ section.settings.grid_heading }}</h2>
      </div>
    {%- endif -%}

    <div class="vs-usage-grid-{{ section.id }}">
      
      <div class="vs-usage-card-{{ section.id }}">
        {%- if section.settings.img1 != blank -%}
          {{ section.settings.img1 | image_url: width: 600 | image_tag: loading: 'lazy', alt: section.settings.lbl1 }}
        {%- else -%}
          <div style="width:100%;height:100%;background:#e2e8f0;display:flex;align-items:center;justify-content:center;color:#94a3b8;">Image 1</div>
        {%- endif -%}
        <div class="vs-usage-overlay-{{ section.id }}">
          <span>{{ section.settings.lbl1 }}</span>
          <span class="badge-tag">Residential</span>
        </div>
      </div>
      
      <div class="vs-usage-card-{{ section.id }}">
        {%- if section.settings.img2 != blank -%}
          {{ section.settings.img2 | image_url: width: 600 | image_tag: loading: 'lazy', alt: section.settings.lbl2 }}
        {%- else -%}
          <div style="width:100%;height:100%;background:#cbd5e1;display:flex;align-items:center;justify-content:center;color:#94a3b8;">Image 2</div>
        {%- endif -%}
        <div class="vs-usage-overlay-{{ section.id }}">
          <span>{{ section.settings.lbl2 }}</span>
          <span class="badge-tag">Commercial</span>
        </div>
      </div>
      
      <div class="vs-usage-card-{{ section.id }}">
        {%- if section.settings.img3 != blank -%}
          {{ section.settings.img3 | image_url: width: 600 | image_tag: loading: 'lazy', alt: section.settings.lbl3 }}
        {%- else -%}
          <div style="width:100%;height:100%;background:#cbd5e1;display:flex;align-items:center;justify-content:center;color:#94a3b8;">Image 3</div>
        {%- endif -%}
        <div class="vs-usage-overlay-{{ section.id }}">
          <span>{{ section.settings.lbl3 }}</span>
          <span class="badge-tag">Industrial</span>
        </div>
      </div>
      
      <div class="vs-usage-card-{{ section.id }}">
        {%- if section.settings.img4 != blank -%}
          {{ section.settings.img4 | image_url: width: 600 | image_tag: loading: 'lazy', alt: section.settings.lbl4 }}
        {%- else -%}
          <div style="width:100%;height:100%;background:#e2e8f0;display:flex;align-items:center;justify-content:center;color:#94a3b8;">Image 4</div>
        {%- endif -%}
        <div class="vs-usage-overlay-{{ section.id }}">
          <span>{{ section.settings.lbl4 }}</span>
          <span class="badge-tag">Institutional</span>
        </div>
      </div>

    </div>
    
  </div>
</section>

<script>
  document.addEventListener("DOMContentLoaded", function() {

    // --- Added: Precise Timeline Line Calculator ---
    function adjustTimelineLine() {
      const section = document.querySelector('.vs-section-{{ section.id }}');
      if (!section) return;
      
      const timeline = section.querySelector('.timeline');
      const line = section.querySelector('.timeline-line');
      const icons = section.querySelectorAll('.item .icon');
      
      if (timeline && line && icons.length > 1) {
        const firstIcon = icons[0];
        const lastIcon = icons[icons.length - 1];
        const timelineRect = timeline.getBoundingClientRect();
        
        // Calculate dynamic start point (center of the first icon)
        const startY = (firstIcon.getBoundingClientRect().top - timelineRect.top) + (firstIcon.offsetHeight / 2);
        // Calculate dynamic end point (center of the last icon)
        const endY = (lastIcon.getBoundingClientRect().top - timelineRect.top) + (lastIcon.offsetHeight / 2);
        
        line.style.top = startY + 'px';
        line.style.height = (endY - startY) + 'px';
      }
    }

    // Run on layout rendering and resize
    adjustTimelineLine();
    window.addEventListener('resize', adjustTimelineLine);
    setTimeout(adjustTimelineLine, 300); // Safety check for custom fonts pushing content down
    document.addEventListener('shopify:section:load', adjustTimelineLine); // Keeps it working in Theme Editor

    // --- Existing: 3D Image Hover Effect ---
    if (window.innerWidth > 768) {
      document.querySelectorAll('.vs-section-{{ section.id }} .vs-usage-card-{{ section.id }}').forEach(card => {
        card.addEventListener('mousemove', e => {
          const r = card.getBoundingClientRect();
          const rx = ((e.clientY - r.top  - r.height / 2) / (r.height / 2)) * -10;
          const ry = ((e.clientX - r.left - r.width  / 2) / (r.width  / 2)) * 10;
          card.style.transform = `perspective(1000px) translateY(-5px) rotateX(${rx}deg) rotateY(${ry}deg)`;
        });
        card.addEventListener('mouseleave', () => {
          card.style.transform = 'perspective(1000px) rotateX(0deg) rotateY(0deg)';
        });
      });
    }
  });
</script>

{% schema %}
{
  "name": "Premium Timeline",
  "settings": [
    {
      "type": "header",
      "content": "Heading Settings"
    },
    {
      "type": "text",
      "id": "heading_top",
      "label": "Heading Top Line",
      "default": "WHY CHOOSE"
    },
    {
      "type": "text",
      "id": "heading_bottom",
      "label": "Heading Bottom Line",
      "default": "VIDHYUT SAATHI?"
    },
    {
      "type": "textarea",
      "id": "heading_sub",
      "label": "Subheading Paragraph",
      "default": "India’s Premium Energy‑Tech Innovation for Smarter Electricity Optimization"
    },
    {
      "type": "header",
      "content": "Colors Settings"
    },
    {
      "type": "color",
      "id": "bg_color_top",
      "label": "Background Color Top",
      "default": "#fffdf8"
    },
    {
      "type": "color",
      "id": "bg_color_mid",
      "label": "Background Color Middle",
      "default": "#fff8ef"
    },
    {
      "type": "color",
      "id": "bg_color_bottom",
      "label": "Background Color Bottom",
      "default": "#ffffff"
    },
    {
      "type": "header",
      "content": "Image Grid Settings"
    },
    {
      "type": "text",
      "id": "grid_heading",
      "label": "Grid Section Heading",
      "default": "APPLICATIONS OVERVIEW",
      "info": "This adds a title right above the image grid."
    },
    {
      "type": "range",
      "id": "hover_speed",
      "min": 0.2,
      "max": 1.0,
      "step": 0.1,
      "label": "Image Hover Speed",
      "default": 0.5
    },
    {
      "type": "color",
      "id": "overlay_hover_color",
      "label": "Overlay Hover Text Color",
      "default": "#ffbc00"
    },
    {
      "type": "image_picker",
      "id": "img1",
      "label": "Image 1"
    },
    {
      "type": "text",
      "id": "lbl1",
      "label": "Label 1",
      "default": "Residential Use"
    },
    {
      "type": "image_picker",
      "id": "img2",
      "label": "Image 2"
    },
    {
      "type": "text",
      "id": "lbl2",
      "label": "Label 2",
      "default": "Commercial Use"
    },
    {
      "type": "image_picker",
      "id": "img3",
      "label": "Image 3"
    },
    {
      "type": "text",
      "id": "lbl3",
      "label": "Label 3",
      "default": "Industrial Use"
    },
    {
      "type": "image_picker",
      "id": "img4",
      "label": "Image 4"
    },
    {
      "type": "text",
      "id": "lbl4",
      "label": "Label 4",
      "default": "Institutional Use"
    }
  ],
  "blocks": [
    {
      "type": "timeline_item",
      "name": "Timeline Item",
      "settings": [
        {
          "type": "html",
          "id": "custom_icon_svg",
          "label": "Custom SVG Icon Code",
          "info": "Paste custom <svg>...</svg> code here. Leaves blank to use image or default shield."
        },
        {
          "type": "image_picker",
          "id": "custom_icon_image",
          "label": "Custom Icon Image",
          "info": "Used if SVG code above is empty."
        },
        {
          "type": "text",
          "id": "title",
          "label": "Item Title",
          "default": "<span>Based On US-German</span><span>Nano Technology</span>",
          "info": "Wrap text in <span> segments if you want lines to separate exactly like the template design."
        },
        {
          "type": "textarea",
          "id": "text",
          "label": "Item Description",
          "default": "Advanced US‑German nano technology designed for optimized electricity efficiency, stable performance and smarter energy management."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Premium Timeline",
      "blocks": [
        {
          "type": "timeline_item",
          "settings": {
            "title": "<span>Based On US-German</span><span>Nano Technology</span>",
            "text": "Advanced US‑German nano technology designed for optimized electricity efficiency, stable performance and smarter energy management.",
            "custom_icon_svg": "<svg viewBox=\"0 0 24 24\"><path d=\"M12 2L3 7v10l9 5 9-5V7l-9-5z\"/></svg>"
          }
        },
        {
          "type": "timeline_item",
          "settings": {
            "title": "<span>Made With Terahertz Chip</span><span>& 20,000+ Negative Ions</span>",
            "text": "Built with advanced terahertz chip technology and 20,000+ negative ions for smart energy optimization and efficient electrical performance.",
            "custom_icon_svg": "<svg viewBox=\"0 0 24 24\"><path d=\"M13 2L4 14h6v8l9-12h-6z\"/></svg>"
          }
        },
        {
          "type": "timeline_item",
          "settings": {
            "title": "100% Legal & ISO Certified",
            "text": "Completely legal, reliable and designed to follow quality standards for safe and trusted usage.",
            "custom_icon_svg": "<svg viewBox=\"0 0 24 24\"><path d=\"M12 2l8 4v6c0 5-3 10-8 12-5-2-8-7-8-12V6z\"/></svg>"
          }
        }
      ]
    }
  ]
}
{% endschema %}