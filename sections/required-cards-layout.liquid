<style>
/* ============================================================
   VIDHYUT SAATHI — CARD CALCULATOR PAGE
   Desktop & Mobile Responsive Section
   ============================================================ */
.vscc {
  --bg:       #faf7ee;
  --bg-warm:  #f3efe1;
  --panel:    #ffffff;
  --panel-2:  #fbf8f0;
  --line:     #e5e0cf;
  --line-s:   #eeead9;
  --green:    #0e6b3e;
  --green-d:  #0a4a2c;
  --green-t:  #e6f2ea;
  --orange:   #f0631b;
  --orange-t: #fdece1;
  --gold:     #c9982e;
  --ink:      #16281f;
  --muted:    #5e7568;
  --muted-2:  #8ba095;
  --tap:      48px;
  --maxw:     1140px;

  background: var(--bg);
  color: var(--ink);
  font-family: 'Inter', 'Plus Jakarta Sans', sans-serif;
  -webkit-font-smoothing: antialiased;
  overflow-x: hidden;
  font-size: 16px;
}
.vscc *, .vscc *::before, .vscc *::after {
  box-sizing: border-box;
  -webkit-tap-highlight-color: transparent;
}
.vscc h1, .vscc h2, .vscc h3 {
  font-family: 'Space Grotesk', 'Plus Jakarta Sans', sans-serif;
  letter-spacing: -.02em;
}
.vscc a { color: inherit; text-decoration: none; }
.vscc img { max-width: 100%; display: block; }
.vscc button { font-family: inherit; cursor: pointer; }

.vscc .wrap {
  max-width: var(--maxw);
  margin: 0 auto;
  padding: 0 18px;
}
@media(min-width:640px){ .vscc .wrap { padding: 0 32px; } }

/* ---- TEXTURE ---- */
.vscc .texture-bg {
  position: absolute; inset: 0; z-index: 0;
  background-image:
    linear-gradient(var(--line-s) 1px, transparent 1px),
    linear-gradient(90deg, var(--line-s) 1px, transparent 1px);
  background-size: 36px 36px;
  -webkit-mask-image: radial-gradient(ellipse 80% 60% at 50% 10%, black 30%, transparent 80%);
          mask-image: radial-gradient(ellipse 80% 60% at 50% 10%, black 30%, transparent 80%);
}

/* ---- HERO ---- */
.vscc .hero {
  position: relative;
  padding: 40px 0 36px;
  overflow: hidden;
}
@media(min-width:760px){ .vscc .hero { padding: 72px 0 56px; } }

.vscc .eyebrow {
  display: inline-flex; align-items: center; gap: 8px;
  color: var(--green); font-size: 11px; letter-spacing: .08em;
  font-family: 'IBM Plex Mono', monospace;
  text-transform: uppercase; font-weight: 600;
  border: 1px solid var(--green);
  padding: 8px 16px; border-radius: 999px;
  margin-bottom: 20px; background: var(--green-t);
}
.vscc .eyebrow .dot {
  width: 7px; height: 7px; border-radius: 50%;
  background: var(--orange); flex-shrink: 0;
  box-shadow: 0 0 0 3px var(--orange-t);
}

.vscc .hero h1 {
  font-size: clamp(28px, 7vw, 56px);
  line-height: 1.12; font-weight: 700;
  margin: 0 0 18px; max-width: 800px;
}
.vscc .hero h1 em { font-style: normal; color: var(--orange); }

.vscc .hero .lead {
  color: var(--muted);
  font-size: clamp(14.5px, 3.5vw, 17px);
  line-height: 1.65; max-width: 560px;
  margin: 0 0 28px;
}

.vscc .hero-ctas { display: flex; gap: 12px; flex-wrap: wrap; margin-bottom: 32px; }
.vscc .btn-primary {
  background: var(--orange); color: #fff; font-weight: 700;
  padding: 16px 28px; border-radius: 14px;
  font-size: 16px; border: none;
  box-shadow: 0 8px 24px -8px rgba(240,99,27,.45);
  min-height: 52px;
  display: inline-flex; align-items: center; justify-content: center;
  transition: transform .15s, box-shadow .15s;
}
.vscc .btn-primary:hover { transform: translateY(-1px); box-shadow: 0 12px 28px -8px rgba(240,99,27,.5); }
.vscc .btn-ghost {
  border: 1.5px solid var(--green); color: var(--green);
  padding: 16px 28px; border-radius: 14px;
  font-size: 16px; font-weight: 700;
  min-height: 52px;
  display: inline-flex; align-items: center; justify-content: center;
  background: #fff;
  transition: background .15s, color .15s;
}
.vscc .btn-ghost:hover { background: var(--green-t); }

.vscc .hero-features-strip {
  display: grid; grid-template-columns: repeat(2, 1fr); gap: 12px 20px;
  padding-top: 24px; border-top: 1px solid var(--line);
  font-size: 13.5px; color: var(--muted);
}
@media(min-width:640px){
  .vscc .hero-features-strip { grid-template-columns: repeat(4, auto); justify-content: start; gap: 32px; }
}
.vscc .hf-item { display: flex; align-items: center; gap: 8px; }
.vscc .hf-dot { width: 6px; height: 6px; border-radius: 50%; background: var(--orange); flex-shrink: 0; }
.vscc .hf-item b { color: var(--green); font-weight: 700; }

/* ---- HERO DESKTOP GRID & GENTLEMAN ADVISOR IMAGE ---- */
.vscc .hero-grid-container {
  display: grid;
  grid-template-columns: 1fr;
  gap: 32px;
  align-items: center;
}

@media(min-width: 900px) {
  .vscc .hero-grid-container {
    grid-template-columns: 1.1fr 0.9fr;
    gap: 44px;
  }
}

.vscc .hero-right-col {
  display: none !important; /* Hidden on mobile theme */
}

@media(min-width: 900px) {
  .vscc .hero-right-col {
    display: block !important;
    position: relative;
  }
}

.vscc .hero-image-frame {
  position: relative;
  border-radius: 24px;
  overflow: hidden;
  box-shadow: 0 20px 44px -12px rgba(10, 74, 44, 0.25), 0 4px 16px rgba(0,0,0,0.06);
  border: 2px solid var(--line);
  background: #ffffff;
}

.vscc .hero-advisor-img {
  width: 100%;
  height: auto;
  aspect-ratio: 4 / 3;
  object-fit: cover;
  display: block;
  transition: transform 0.4s ease;
}
.vscc .hero-image-frame:hover .hero-advisor-img {
  transform: scale(1.03);
}

.vscc .hero-img-badge {
  position: absolute;
  bottom: 20px;
  left: 20px;
  background: rgba(255, 255, 255, 0.95);
  -webkit-backdrop-filter: blur(10px);
  backdrop-filter: blur(10px);
  border: 1.5px solid var(--line);
  padding: 10px 16px;
  border-radius: 14px;
  display: flex;
  align-items: center;
  gap: 12px;
  box-shadow: 0 8px 24px rgba(0,0,0,0.12);
}

.vscc .badge-icon {
  width: 32px;
  height: 32px;
  border-radius: 50%;
  background: #0a4a2c;
  color: #ffffff;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 15px;
  flex-shrink: 0;
}

.vscc .badge-title {
  font-family: 'Space Grotesk', sans-serif;
  font-weight: 700;
  font-size: 13px;
  color: var(--ink);
  line-height: 1.2;
}

.vscc .badge-sub {
  font-size: 11px;
  color: var(--muted);
  font-family: 'Inter', sans-serif;
}

/* ---- SECTIONS ---- */
.vscc .sec { padding: 52px 0; }
@media(min-width:760px){ .vscc .sec { padding: 72px 0; } }

.vscc .sec-head { max-width: 640px; margin: 0 0 32px; }
.vscc .sec-eyebrow {
  font-size: 11px; letter-spacing: .14em;
  text-transform: uppercase; color: var(--orange);
  margin-bottom: 8px; font-weight: 700;
}
.vscc .sec h2 { font-size: clamp(22px, 5vw, 32px); margin: 0 0 10px; font-weight: 700; }
.vscc .sec p.desc { color: var(--muted); font-size: 15px; line-height: 1.65; margin: 0; }

/* ---- CALCULATOR ---- */
.vscc .calc-outer-box {
  position: relative;
  background: #fbf8f0;
  border: 1.5px solid #f0631b;
  border-radius: 24px;
  padding: 36px 20px 24px;
  margin-top: 16px;
  box-shadow: 0 4px 20px rgba(240,99,27,0.06);
}
@media(min-width:640px){
  .vscc .calc-outer-box { padding: 40px 32px 32px; }
}

@media(min-width: 900px) {
  .vscc .calc-outer-box {
    display: grid !important;
    grid-template-columns: 1fr 1fr !important;
    gap: 36px !important;
    background: transparent !important;
    border: none !important;
    box-shadow: none !important;
    padding: 0 !important;
    align-items: stretch !important;
  }

  .vscc .c-inputs-card {
    background: #fbf8f0 !important;
    border: 1.5px solid #f0631b !important;
    border-radius: 24px !important;
    padding: 36px 32px 32px !important;
    box-shadow: 0 4px 24px rgba(240,99,27,0.08) !important;
    position: relative !important;
    display: flex !important;
    flex-direction: column !important;
    justify-content: space-between !important;
  }

  .vscc .c-output-card {
    background: #ffffff !important;
    border: 1.5px solid var(--line) !important;
    border-radius: 24px !important;
    padding: 36px 32px 32px !important;
    box-shadow: 0 12px 36px rgba(10,74,44,0.08) !important;
    position: relative !important;
    display: flex !important;
    flex-direction: column !important;
  }
}

.vscc .live-calc-badge {
  position: absolute;
  top: -14px;
  left: 24px;
  background: #e8c766;
  color: #16281f;
  font-size: 11px;
  font-weight: 800;
  letter-spacing: .08em;
  text-transform: uppercase;
  padding: 5px 14px;
  border-radius: 999px;
  border: 1.5px solid #c9982e;
  box-shadow: 0 2px 8px rgba(0,0,0,0.08);
  display: inline-flex;
  align-items: center;
  gap: 6px;
  font-family: 'IBM Plex Mono', monospace;
}

.vscc .field {
  display: flex !important;
  flex-direction: column !important;
  width: 100% !important;
  margin-bottom: 24px !important;
  clear: both !important;
  box-sizing: border-box !important;
}
.vscc .field:last-child { margin-bottom: 0 !important; }

.vscc .field-label {
  display: flex !important;
  flex-direction: row !important;
  justify-content: space-between !important;
  align-items: center !important;
  width: 100% !important;
  margin-bottom: 12px !important;
  float: none !important;
  box-sizing: border-box !important;
}
.vscc .field-label > span:first-child {
  font-family: 'IBM Plex Mono', monospace !important;
  font-weight: 700 !important;
  font-size: 11.5px !important;
  letter-spacing: .04em !important;
  text-transform: uppercase !important;
  color: var(--ink) !important;
  white-space: nowrap !important;
}
.vscc .field-val {
  display: inline-flex !important;
  align-items: center !important;
  justify-content: center !important;
  background: var(--orange-t) !important;
  color: var(--orange) !important;
  font-size: 14px !important;
  font-weight: 700 !important;
  padding: 4px 12px !important;
  border-radius: 8px !important;
  font-family: 'IBM Plex Mono', monospace !important;
  flex-shrink: 0 !important;
  white-space: nowrap !important;
  float: none !important;
}

.vscc input[type=range] {
  display: block !important;
  width: 100% !important;
  -webkit-appearance: none !important;
  appearance: none !important;
  height: 8px !important;
  border-radius: 999px !important;
  background: linear-gradient(90deg, var(--green) 0%, var(--green) var(--fill,10%), var(--line) var(--fill,10%)) !important;
  outline: none !important;
  margin: 10px 0 14px 0 !important;
  touch-action: pan-y !important;
  box-sizing: border-box !important;
  float: none !important;
}
.vscc input[type=range]::-webkit-slider-thumb {
  -webkit-appearance: none !important;
  width: 28px !important;
  height: 28px !important;
  border-radius: 50% !important;
  background: var(--orange) !important;
  border: 4px solid #fff !important;
  box-shadow: 0 2px 8px rgba(0,0,0,.25) !important;
  cursor: pointer !important;
}
.vscc input[type=range]::-moz-range-thumb {
  width: 28px !important;
  height: 28px !important;
  border-radius: 50% !important;
  background: var(--orange) !important;
  border: 4px solid #fff !important;
  box-shadow: 0 2px 8px rgba(0,0,0,.25) !important;
  cursor: pointer !important;
}

.vscc .field-input-box {
  display: block !important;
  width: 100% !important;
  clear: both !important;
  margin-top: 6px !important;
  box-sizing: border-box !important;
}

.vscc .num-input {
  display: block !important;
  width: 100% !important;
  min-width: 100% !important;
  height: 52px !important;
  min-height: 52px !important;
  background: #ffffff !important;
  border: 1.5px solid #e5e0cf !important;
  color: var(--ink) !important;
  font-family: 'IBM Plex Mono', monospace !important;
  font-size: 16px !important;
  font-weight: 700 !important;
  padding: 12px 16px !important;
  border-radius: 12px !important;
  outline: none !important;
  box-sizing: border-box !important;
  float: none !important;
  box-shadow: inset 0 1px 3px rgba(0,0,0,0.04) !important;
  transition: border-color .15s, box-shadow .15s !important;
}
.vscc .num-input:focus {
  border-color: #f0631b !important;
  background: #ffffff !important;
  box-shadow: 0 0 0 3px rgba(240,99,27,0.15) !important;
}

.vscc .prefix-wrap {
  position: relative !important;
  display: block !important;
  width: 100% !important;
  clear: both !important;
  box-sizing: border-box !important;
}
.vscc .prefix-wrap .pfx {
  position: absolute !important;
  left: 16px !important;
  top: 50% !important;
  transform: translateY(-50%) !important;
  color: var(--muted-2) !important;
  font-family: 'IBM Plex Mono', monospace !important;
  pointer-events: none !important;
  font-size: 18px !important;
  font-weight: 700 !important;
  z-index: 2 !important;
}
.vscc .prefix-wrap input.num-input {
  padding-left: 36px !important;
}

.vscc .toggle-grp { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; width: 100%; }
.vscc .tog-btn {
  padding: 14px 10px; border-radius: 12px;
  border: 1.5px solid var(--line);
  background: #ffffff; color: var(--muted);
  font-weight: 700; font-size: 13.5px;
  min-height: 50px;
  display: flex; align-items: center; justify-content: center; gap: 6px;
  transition: all .15s;
  white-space: nowrap;
}
.vscc .tog-btn.active {
  border-color: #0a4a2c; color: #ffffff;
  background: #0a4a2c;
  box-shadow: 0 4px 12px rgba(10,74,44,.2);
}

/* Savings Box */
.vscc .savings-box {
  background: linear-gradient(135deg, #0a4a2c, #0e6b3e);
  border-radius: 16px;
  padding: 16px 18px;
  color: #ffffff;
  margin: 18px 0;
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 12px;
  box-shadow: 0 4px 16px rgba(10,74,44,0.2);
}
.vscc .savings-label {
  font-size: 10px;
  font-weight: 700;
  letter-spacing: .08em;
  color: #cfe4d7;
  text-transform: uppercase;
  font-family: 'IBM Plex Mono', monospace;
  margin-bottom: 4px;
  white-space: nowrap;
}
.vscc .savings-amount {
  font-family: 'Space Grotesk', 'IBM Plex Mono', monospace;
  font-weight: 800;
  font-size: clamp(16px, 4.5vw, 22px);
  color: #ffffff;
  white-space: nowrap;
}
.vscc .savings-icon {
  font-size: 26px;
  flex-shrink: 0;
}

/* Action Buttons */
.vscc .calc-actions {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 12px;
  margin-bottom: 18px;
}
@media(max-width:480px){
  .vscc .calc-actions { grid-template-columns: 1fr; }
}

.vscc .btn-wa {
  background: #25D366;
  color: #ffffff;
  font-weight: 700;
  font-size: 14px;
  padding: 13px 16px;
  border-radius: 12px;
  border: none;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  text-decoration: none;
  box-shadow: 0 4px 12px rgba(37,211,102,0.25);
  transition: transform .15s;
}
.vscc .btn-wa:hover { transform: translateY(-1px); }

.vscc .btn-quote {
  background: #ffffff;
  color: #0a4a2c;
  border: 1.5px solid #0e6b3e;
  font-weight: 700;
  font-size: 14px;
  padding: 13px 16px;
  border-radius: 12px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  text-decoration: none;
  transition: background .15s;
}
.vscc .btn-quote:hover { background: #e6f2ea; }

/* output side */
.vscc .c-output {
  padding: 28px 24px;
  background: var(--panel-2);
  display: flex; flex-direction: column; gap: 0;
}
@media(min-width:520px){ .vscc .c-output { padding: 34px 32px; } }
@media(min-width:860px){ .vscc .c-output { padding: 40px 36px; background: var(--panel); } }

.vscc .res-label {
  font-size: 11px; font-weight: 700; letter-spacing: .14em;
  text-transform: uppercase; color: var(--muted-2); margin-bottom: 6px;
}
.vscc .res-number {
  font-family: 'Space Grotesk', sans-serif; font-weight: 700;
  font-size: clamp(52px, 14vw, 72px); line-height: 1;
  color: var(--green-d);
  display: flex; align-items: baseline; gap: 10px; flex-wrap: wrap;
  margin-bottom: 20px;
}
.vscc .res-number .unit { font-size: 16px; color: var(--muted); font-weight: 600; font-family: 'Inter'; }

/* ---- REAL 3D CARD COMPONENT (STATIC 3D ANGLE) ---- */
.vscc .vscc-3d-container {
  perspective: 1000px;
  width: 100%;
  max-width: 380px;
  margin: 16px auto 14px;
  display: flex;
  justify-content: center;
  align-items: center;
}

.vscc .vscc-3d-card-stage {
  width: 100%;
  aspect-ratio: 1.586 / 1;
  position: relative;
  transform-style: preserve-3d;
  transform: rotateY(-6deg) rotateX(4deg);
  border-radius: 16px;
  box-shadow: 0 16px 36px rgba(0,0,0,0.28), 0 6px 16px rgba(240,99,27,0.2);
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}
.vscc .vscc-3d-container:hover .vscc-3d-card-stage {
  transform: rotateY(0deg) rotateX(0deg) scale(1.02);
  box-shadow: 0 20px 44px rgba(0,0,0,0.32), 0 8px 20px rgba(240,99,27,0.25);
}

.vscc .vscc-3d-face {
  position: absolute;
  inset: 0;
  border-radius: 16px;
  overflow: hidden;
  border: 1px solid rgba(255,255,255,0.25);
  background: #0d2238;
}

.vscc .vscc-3d-face img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

.vscc .vscc-3d-glare {
  position: absolute;
  inset: 0;
  border-radius: 16px;
  background: linear-gradient(115deg, transparent 35%, rgba(255,255,255,0.22) 50%, transparent 65%);
  pointer-events: none;
  z-index: 3;
}

.vscc .vscc-3d-ribbon {
  position: absolute;
  top: 12px;
  right: -32px;
  background: var(--orange);
  color: #ffffff;
  font-size: 9.5px;
  font-weight: 800;
  letter-spacing: .05em;
  padding: 4px 36px;
  transform: rotate(28deg);
  box-shadow: 0 2px 6px rgba(0,0,0,0.25);
  z-index: 4;
}

/* Dynamic Mini Cards Row */
.vscc .vscc-mini-cards-row {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 6px;
  margin: 14px 0 18px 0;
}

.vscc .vscc-mini-card-icon {
  width: 32px;
  height: 20px;
  border-radius: 5px;
  background: linear-gradient(135deg, #e8c766 0%, #c9982e 100%);
  border: 1px solid #b8860b;
  box-shadow: 0 2px 6px rgba(0,0,0,0.18);
  transition: transform 0.18s ease;
}
.vscc .vscc-mini-card-icon:hover {
  transform: translateY(-2px) scale(1.1);
}

.vscc .chip-meter { display: flex; flex-wrap: wrap; gap: 5px; margin-bottom: 18px; }
.vscc .chip-dot {
  width: 22px; height: 15px; border-radius: 3px;
  background: var(--line); border: 1px solid var(--line);
  transition: background .2s, border-color .2s;
}
.vscc .chip-dot.on { background: linear-gradient(135deg, #e8c766, var(--gold)); border-color: var(--gold); }

.vscc .breakdown {
  background: var(--panel); border: 1.5px solid var(--line);
  border-radius: 13px; padding: 16px;
  font-size: 13.5px; color: var(--muted); line-height: 1.9;
  margin-bottom: 16px;
}
@media(min-width:860px){ .vscc .breakdown { background: var(--panel-2); } }
.vscc .brow { display: flex; justify-content: space-between; gap: 10px; align-items: center; }
.vscc .brow b { color: var(--ink); font-family: 'IBM Plex Mono', monospace; font-weight: 700; font-size: 13px; }
.vscc .brow.win b { color: var(--green); }
.vscc .bdivider { border: none; border-top: 1px dashed var(--line); margin: 8px 0; }

.vscc .calc-note {
  font-size: 12px; color: var(--muted-2); line-height: 1.65; margin-top: auto;
  padding: 12px 14px; background: var(--green-t);
  border-radius: 10px; border: 1px solid rgba(14,107,62,.2);
}/* ---- SECTION 3 REQUIREMENT CHART TABLE (EXACT MATCH FOR USER MOBILE PHOTO) ---- */
.vscc .chart-outer-box {
  background: #ffffff;
  border: 1.5px solid #e5e0cf;
  border-radius: 18px;
  overflow: hidden;
  box-shadow: 0 4px 20px rgba(0,0,0,0.04);
  margin-top: 20px;
}

.vscc .chart-table-green {
  width: 100%;
  border-collapse: collapse;
  background: #ffffff;
}

.vscc .chart-table-green thead tr {
  background: #0a4a2c;
}

.vscc .chart-table-green thead th {
  color: #ffffff;
  font-family: 'IBM Plex Mono', monospace;
  font-size: 11.5px;
  font-weight: 700;
  letter-spacing: .06em;
  text-transform: uppercase;
  padding: 13px 12px;
  white-space: nowrap;
}

.vscc .chart-table-green tbody tr {
  border-bottom: 1px solid #eeead9;
  transition: background .15s;
}
.vscc .chart-table-green tbody tr:last-child {
  border-bottom: none;
}
.vscc .chart-table-green tbody tr:nth-child(even) {
  background: #fbf8f0;
}
.vscc .chart-table-green tbody tr:hover {
  background: #f5f0e4;
}

.vscc .chart-table-green td {
  padding: 13px 12px;
  color: var(--muted);
  vertical-align: middle;
  font-size: 13.5px;
}

.vscc .chart-table-green td:nth-child(1) {
  font-family: 'Space Grotesk', 'IBM Plex Mono', monospace;
  font-weight: 800;
  color: var(--ink);
  font-size: 14px;
  text-align: left;
  white-space: nowrap;
}

.vscc .chart-table-green td:nth-child(2) {
  font-family: 'Inter', sans-serif;
  color: #5e7568;
  font-size: 13px;
  text-align: center;
  white-space: nowrap;
}

.vscc .chart-table-green td:nth-child(3),
.vscc .chart-table-green td:nth-child(4) {
  font-family: 'IBM Plex Mono', monospace;
  font-weight: 800;
  color: #f0631b;
  font-size: 14px;
  text-align: center;
  white-space: nowrap;
}

@media(max-width: 600px) {
  .vscc .chart-table-green thead th {
    font-size: 10.5px;
    padding: 11px 8px;
  }
  .vscc .chart-table-green td {
    padding: 11px 8px;
    font-size: 12.5px;
  }
  .vscc .chart-table-green td:nth-child(1) {
    font-size: 12.5px;
  }
  .vscc .chart-table-green td:nth-child(2) {
    font-size: 12px;
  }
  .vscc .chart-table-green td:nth-child(3),
  .vscc .chart-table-green td:nth-child(4) {
    font-size: 13px;
  }
}

/* rule box */
.vscc .rule-box {
  display: flex; gap: 14px; align-items: flex-start;
  background: var(--green-t); border: 1.5px solid var(--green);
  border-radius: 14px; padding: 18px 20px; margin-top: 20px;
  font-size: 13.5px; color: var(--green-d); line-height: 1.65;
}
.vscc .rule-icon {
  width: 38px; height: 38px; border-radius: 50%; flex-shrink: 0;
  background: linear-gradient(135deg, var(--green-d), var(--green));
  color: #fff; display: flex; align-items: center; justify-content: center;
  font-size: 18px; box-shadow: 0 4px 12px rgba(14,107,62,.25);
}
.vscc .rule-box b { color: var(--orange); }

/* ---- EXAMPLES ---- */
.vscc .tabs { display: flex; gap: 8px; flex-wrap: wrap; margin-bottom: 24px; }
.vscc .tab-btn {
  padding: 11px 18px; border-radius: 999px;
  border: 1.5px solid var(--line);
  background: #fff; color: var(--muted);
  font-weight: 700; font-size: 13px; min-height: 44px;
  transition: all .15s;
}
.vscc .tab-btn:hover { border-color: var(--green-d); color: var(--green-d); }
.vscc .tab-btn.active {
  background: linear-gradient(135deg, var(--green-d), var(--green));
  border-color: var(--green); color: #fff;
  box-shadow: 0 6px 18px rgba(14,107,62,.25);
}
.vscc .eg-grid { display: grid; grid-template-columns: 1fr; gap: 16px; }
@media(min-width:680px){ .vscc .eg-grid { grid-template-columns: 1fr 1fr; gap: 20px; } }
.vscc .eg-panel { display: none; }
.vscc .eg-panel.active { display: grid; }
.vscc .eg-card {
  background: var(--panel); border: 1.5px solid var(--line);
  border-radius: 18px; padding: 22px;
  transition: border-color .2s, transform .2s, box-shadow .2s;
}
.vscc .eg-card:hover {
  border-color: rgba(240,99,27,.4);
  transform: translateY(-3px);
  box-shadow: 0 16px 40px -12px rgba(240,99,27,.12);
}
.vscc .eg-card h4 { margin: 0 0 4px; font-size: 16.5px; font-weight: 800; color: var(--ink); }
.vscc .eg-sub {
  color: var(--muted-2); font-size: 11.5px; margin-bottom: 16px;
  font-family: 'IBM Plex Mono', monospace; letter-spacing: .04em;
}
.vscc .eg-stats { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; margin-bottom: 14px; }
.vscc .eg-stat {
  background: var(--panel-2); border: 1px solid var(--line);
  border-radius: 10px; padding: 12px 14px;
}
.vscc .eg-stat .k { color: var(--muted-2); font-size: 10px; text-transform: uppercase; letter-spacing: .06em; font-weight: 700; margin-bottom: 4px; }
.vscc .eg-stat .v { font-family: 'IBM Plex Mono', monospace; font-weight: 700; font-size: 15.5px; color: var(--ink); }
.vscc .eg-result {
  display: flex; justify-content: space-between; align-items: center;
  background: var(--orange-t); border: 1.5px solid rgba(240,99,27,.35);
  border-radius: 11px; padding: 13px 16px; margin-bottom: 12px;
}
.vscc .eg-result .rl { font-size: 12px; color: var(--orange); font-weight: 700; }
.vscc .eg-result .rn { font-family: 'IBM Plex Mono', monospace; font-weight: 700; color: var(--orange); font-size: 20px; }
.vscc .eg-why {
  font-size: 12.5px; color: var(--muted); line-height: 1.6;
  background: var(--panel-2); border-left: 3px solid var(--green);
  padding: 10px 12px; border-radius: 8px;
}

/* ---- BADGES ---- */
.vscc .badges-row {
  display: grid; grid-template-columns: repeat(2, 1fr); gap: 12px;
}
@media(min-width:600px){ .vscc .badges-row { grid-template-columns: repeat(5, 1fr); gap: 1px; background: var(--line); border: 1.5px solid var(--line); border-radius: 18px; overflow: hidden; } }
.vscc .badge-item {
  background: var(--panel); padding: 22px 14px; text-align: center;
  border-radius: 14px; border: 1.5px solid var(--line);
}
@media(min-width:600px){ .vscc .badge-item { border-radius: 0; border: none; padding: 28px 14px; } }
.vscc .badge-item .big { font-family: 'Space Grotesk', sans-serif; font-weight: 700; font-size: 21px; color: var(--orange); }
.vscc .badge-item .lbl { font-size: 11.5px; color: var(--muted); margin-top: 6px; line-height: 1.4; }

/* ---- CTA ---- */
.vscc .cta-band {
  background: linear-gradient(135deg, var(--green-d), var(--green));
  border-radius: 22px; color: #fff; padding: 40px 28px; text-align: center;
  position: relative; overflow: hidden;
}
.vscc .cta-band::before {
  content: ''; position: absolute; top: -80px; right: -80px; width: 260px; height: 260px;
  background: radial-gradient(circle, rgba(255,255,255,.12), transparent 70%); pointer-events: none;
}
@media(min-width:640px){ .vscc .cta-band { padding: 56px 48px; } }
.vscc .cta-band h3 { font-size: clamp(20px, 5vw, 30px); margin: 0 0 14px; }
.vscc .cta-band p { color: rgba(207,228,215,.9); margin: 0 0 28px; font-size: 15px; max-width: 460px; margin-left: auto; margin-right: auto; }
.vscc .cta-band .btn-white {
  background: #fff; color: var(--green-d); font-weight: 800;
  padding: 15px 32px; border-radius: 12px; font-size: 15px;
  display: inline-flex; align-items: center; box-shadow: 0 8px 24px rgba(0,0,0,.15);
  transition: transform .15s;
}
.vscc .cta-band .btn-white:hover { transform: translateY(-2px); }

/* ---- MOBILE STICKY BAR (EXACTLY AS IN USER PHOTO) ---- */
.vscc-sticky-bar {
  position: fixed; left: 0; right: 0; bottom: 0; z-index: 99;
  background: #0a4a2c; color: #fff;
  padding: 12px 18px;
  padding-bottom: calc(12px + env(safe-area-inset-bottom));
  display: flex; align-items: center; justify-content: space-between; gap: 12px;
  box-shadow: 0 -6px 20px rgba(0,0,0,.25);
  transform: translateY(110%);
  transition: transform .28s cubic-bezier(.16,1,.3,1);
}
.vscc-sticky-bar.show { transform: translateY(0); }
@media(min-width:700px){ .vscc-sticky-bar { display: none !important; } }

.vscc-sticky-txt {
  font-size: 10px; font-weight: 700; letter-spacing: .08em;
  color: #cfe4d7; text-transform: uppercase; margin-bottom: 2px;
}
.vscc-sticky-num {
  font-family: 'Space Grotesk', 'IBM Plex Mono', monospace;
  font-weight: 700; font-size: 22px; line-height: 1; color: #ffffff;
}
.vscc-sticky-btn {
  background: #f0631b; color: #ffffff; font-weight: 700; font-size: 13.5px;
  padding: 10px 18px; border-radius: 10px; text-decoration: none;
  display: inline-flex; align-items: center;
  box-shadow: 0 4px 12px rgba(240,99,27,.3); flex-shrink: 0;
}

/* ---- MOBILE OVERRIDES ---- */
@media(max-width:767px){
  .vscc .hero { padding: 30px 0 26px; }
  .vscc .hero h1 { font-size: clamp(24px, 8vw, 36px); }
  .vscc .hero .lead { font-size: 14px; line-height: 1.65; text-align: justify; hyphens: auto; }
  .vscc .hero-ctas { flex-direction: column; gap: 10px; }
  .vscc .btn-primary, .vscc .btn-ghost { width: 100%; font-size: 15px; }

  .vscc .sec { padding: 36px 0; }
  .vscc .sec-head { margin-bottom: 20px; }
  .vscc .sec h2 { font-size: clamp(20px, 6vw, 26px); }
  .vscc .sec p.desc { font-size: 14px; text-align: justify; hyphens: auto; }

  /* calc */
  .vscc .calc-wrap { border-radius: 18px; }
  .vscc .c-inputs { padding: 22px 18px; }
  .vscc .c-output { padding: 22px 18px; }
  .vscc .field { margin-bottom: 20px; }
  .vscc .field-label { font-size: 10.5px; }
  .vscc .num-input { font-size: 15px; padding: 12px 14px; }
  .vscc .tog-btn { font-size: 13px; padding: 13px 8px; }

  /* result */
  .vscc .res-number { font-size: clamp(46px, 14vw, 64px); }
  .vscc .card-mock { padding: 14px 16px; margin-bottom: 16px; }

  /* breakdown */
  .vscc .breakdown { padding: 13px; font-size: 13px; line-height: 1.8; }
  .vscc .calc-note { font-size: 12px; text-align: justify; hyphens: auto; }

  .vscc .rule-box { padding: 14px 16px; font-size: 13px; gap: 10px; text-align: justify; hyphens: auto; }
  .vscc .rule-icon { width: 34px; height: 34px; font-size: 16px; }

  /* examples */
  .vscc .tabs { gap: 8px; margin-bottom: 18px; }
  .vscc .tab-btn { font-size: 12.5px; padding: 10px 14px; min-height: 42px; }
  .vscc .eg-card { padding: 18px 16px; border-radius: 14px; }
  .vscc .eg-card h4 { font-size: 15px; }
  .vscc .eg-sub { font-size: 11px; }
  .vscc .eg-stat .v { font-size: 14px; }
  .vscc .eg-result .rn { font-size: 18px; }
  .vscc .eg-why { font-size: 12px; text-align: justify; hyphens: auto; }

  /* badges */
  .vscc .badges-row { grid-template-columns: repeat(2, 1fr); gap: 10px; }
  .vscc .badge-item { padding: 18px 12px; border-radius: 12px; }
  .vscc .badge-item .big { font-size: 19px; }
  .vscc .badge-item .lbl { font-size: 11px; }

  /* cta */
  .vscc .cta-band { border-radius: 16px; padding: 28px 18px; }
  .vscc .cta-band h3 { font-size: clamp(18px, 6vw, 24px); }
  .vscc .cta-band p { font-size: 13.5px; text-align: justify; hyphens: auto; }
  .vscc .cta-band .btn-white { width: 100%; justify-content: center; padding: 15px; }
}

@media(max-width:380px){
  .vscc .hero h1 { font-size: 23px; }
  .vscc .res-number { font-size: 42px; }
  .vscc .tab-btn { font-size: 12px; padding: 9px 12px; }
}

:focus-visible { outline: 2px solid #f0631b; outline-offset: 2px; }
</style>

<div class="vscc">

  <!-- HERO -->
  <section class="hero">
    <div class="texture-bg"></div>
    <div class="wrap" style="position:relative;z-index:1;">
      <div class="hero-grid-container">
        <!-- LEFT COLUMN: Content -->
        <div class="hero-left-col">
          <div class="eyebrow"><span class="dot"></span> INDIA'S NO. 1 ELECTRIC SAVER CARD COMPANY</div>
          <h1>How many <em>Vidhyut Saathi</em> cards does your connection need?</h1>
          <p class="lead">Enter your connected load and your monthly bill — the calculator checks both and always recommends the higher requirement, exactly the way our installation team does it on site.</p>
          <div class="hero-ctas">
            <a href="#vscc-calc" class="btn-primary">Calculate Now ↓</a>
            <a href="#vscc-chart" class="btn-ghost">View Full Chart</a>
          </div>
          <div class="hero-features-strip">
            <div class="hf-item"><span class="hf-dot"></span> <b>20–30%</b> bill savings</div>
            <div class="hf-item"><span class="hf-dot"></span> <b>10 Yr</b> product life</div>
            <div class="hf-item"><span class="hf-dot"></span> <b>3 Yr</b> warranty</div>
            <div class="hf-item"><span class="hf-dot"></span> <b>ISO</b> certified</div>
          </div>
        </div>

        <!-- RIGHT COLUMN: Gentleman Advisor with Calculator (Desktop Only) -->
        <div class="hero-right-col">
          <div class="hero-image-frame">
            <img src="{{ 'hero-advisor-calculator.jpg' | asset_url }}" alt="Vidhyut Saathi Energy Advisor Showing Calculator Requirements" class="hero-advisor-img">
            <div class="hero-img-badge">
              <span class="badge-icon">⚡</span>
              <div>
                <div class="badge-title">Free Expert Calculation</div>
                <div class="badge-sub">Instant Load & Bill Check</div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- CALCULATOR -->
  <section class="sec" id="vscc-calc">
    <div class="wrap">
      <div class="sec-head">
        <div class="sec-eyebrow">CARD CALCULATOR</div>
        <h2>Get your exact card count</h2>
        <p class="desc">One card saves 20–30% on a small load — bigger connections and heavier bills both call for more coverage. We calculate it both ways and take whichever number is higher.</p>
      </div>

      <div class="calc-outer-box">
        <!-- LEFT CONTAINER: CALCULATION INPUTS -->
        <div class="c-inputs-card">
          <div class="live-calc-badge">⚡ LIVE CALCULATOR</div>

          <!-- KW Slider -->
          <div class="field">
            <div class="field-label">
              <span>⚙️ CONNECTED LOAD</span>
              <span class="field-val" id="vscc-kwVal">10 KW</span>
            </div>
            <input type="range" id="vscc-kwSlider" min="1" max="200" value="10" step="1">
            <div class="field-input-box">
              <input type="number" inputmode="numeric" class="num-input" id="vscc-kwNumber" value="10" min="1" max="2000" placeholder="Enter KW load">
            </div>
          </div>

          <!-- Bill Input -->
          <div class="field">
            <div class="field-label">
              <span>💰 MONTHLY ELECTRICITY BILL</span>
              <span class="field-val" id="vscc-billVal">₹6,000</span>
            </div>
            <div class="field-input-box">
              <div class="prefix-wrap">
                <span class="pfx">₹</span>
                <input type="number" inputmode="numeric" class="num-input" id="vscc-billNumber" value="6000" min="0" step="500" placeholder="Enter monthly bill">
              </div>
            </div>
          </div>

          <!-- Phase Toggle -->
          <div class="field" style="margin-bottom:24px;">
            <div class="field-label">CONNECTION TYPE</div>
            <div class="toggle-grp">
              <button class="tog-btn active" id="vscc-btnSingle">Single Phase</button>
              <button class="tog-btn" id="vscc-btnThree">Three Phase</button>
            </div>
          </div>

          <p class="calc-note" style="background:transparent;border:none;padding:0;color:var(--muted-2);font-size:12px;line-height:1.65;margin-top:auto;">
            Rule of thumb: 1 card per 6 KW of load, or 1 card per ₹6,000 of monthly bill — whichever gives the bigger number. Three-phase connections always get one additional card. Savings and quote figures update instantly as you adjust the sliders.
          </p>
        </div>

        <!-- RIGHT CONTAINER: CARDS OUTPUT & 3D CARD IMAGE -->
        <div class="c-output-card">
          <div class="res-label">CARDS REQUIRED</div>
          <div class="res-number">
            <span id="vscc-resultNum">1</span>
            <span class="unit" id="vscc-resultUnit">Card</span>
          </div>

          <!-- Real 3D Vidhyut Saathi Card (Static 3D Angle) -->
          <div class="vscc-3d-container">
            <div class="vscc-3d-card-stage">
              <div class="vscc-3d-face">
                <img src="{{ 'official-vs-card-front.jpg' | asset_url }}" alt="Vidhyut Saathi Electric Power Saving Card">
                <div class="vscc-3d-glare"></div>
              </div>
            </div>
          </div>

          <!-- Dynamic Mini Gold Cards Row -->
          <div class="vscc-mini-cards-row" id="vscc-miniCardsRow"></div>

          <!-- Breakdown -->
          <div class="breakdown">
            <div class="brow"><span>Based on KW load (⌈KW ÷ 6⌉)</span><b id="vscc-kwCards">1</b></div>
            <div class="brow"><span>Based on bill amount (⌈Bill ÷ ₹6,000⌉)</span><b id="vscc-billCards">1</b></div>
            <hr class="bdivider">
            <div class="brow win"><span>Higher requirement considered</span><b id="vscc-baseWin" style="color:var(--green)">1</b></div>
            <div class="brow" id="vscc-phaseRow" style="display:none;"><span>+ 1 extra for 3-phase connection</span><b style="color:var(--orange)">+1</b></div>
          </div>

          <!-- Estimated Monthly Savings Banner -->
          <div class="savings-box">
            <div>
              <div class="savings-label">ESTIMATED MONTHLY SAVINGS</div>
              <div class="savings-amount" id="vscc-savingsRange">₹1,200 – ₹1,800</div>
            </div>
            <div class="savings-icon">💡</div>
          </div>

          <!-- Action Buttons -->
          <div class="calc-actions">
            <a href="https://wa.me/?text=Hi%2C%20I%20want%20to%20order%20Vidhyut%20Saathi%20Electric%20Saver%20Cards" class="btn-wa" target="_blank">
              <svg width="20" height="20" fill="currentColor" viewBox="0 0 24 24"><path d="M.057 24l1.687-6.163c-1.041-1.804-1.588-3.849-1.587-5.946.003-6.556 5.338-11.891 11.893-11.891 3.181.001 6.167 1.24 8.413 3.488 2.245 2.248 3.481 5.236 3.48 8.414-.003 6.557-5.338 11.892-11.893 11.892-1.99-.001-3.951-.5-5.688-1.448l-6.305 1.654zm6.597-3.807c1.676.995 3.276 1.591 5.392 1.592 5.448 0 9.886-4.434 9.889-9.885.002-5.462-4.415-9.89-9.881-9.892-5.452 0-9.887 4.434-9.889 9.884-.001 2.225.651 3.891 1.746 5.634l-.999 3.648 3.742-.981z"/></svg>
              Order on WhatsApp
            </a>
            <a href="tel:+919876543210" class="btn-quote">
              <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/></svg>
              Call for Quote
            </a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- FULL REQUIREMENT CHART -->
  <section class="sec" id="vscc-chart" style="padding-top:10px;">
    <div class="wrap">
      <div class="sec-head">
        <div class="sec-eyebrow" style="color:var(--orange);font-family:'IBM Plex Mono',monospace;letter-spacing:.12em;font-size:11px;">REFERENCE</div>
        <h2 style="font-family:'Space Grotesk','Plus Jakarta Sans',sans-serif;font-size:clamp(24px, 5vw, 32px);font-weight:700;margin:4px 0 10px;">Full requirement chart</h2>
        <p class="desc" style="color:var(--muted);font-size:14.5px;line-height:1.6;margin:0;">The same logic as the calculator above, laid out by connection size — from a single home to a full industrial load.</p>
      </div>

      <div class="chart-outer-box">
        <table class="chart-table-green">
          <thead>
            <tr>
              <th style="text-align:left;">KW</th>
              <th style="text-align:center;">BILL (₹)</th>
              <th style="text-align:center;">CARDS</th>
              <th style="text-align:center;">3-PH</th>
            </tr>
          </thead>
          <tbody id="vscc-chartBody"></tbody>
        </table>
      </div>

      <div class="rule-box" style="margin-top:28px;">
        <div class="rule-icon">⚡</div>
        <span><b>Always calculate by both</b> KW load and bill amount, and take the higher of the two. Add <b>1 extra card</b> automatically for any three-phase connection.</span>
      </div>
    </div>
  </section>

  <!-- WORKED EXAMPLES -->
  <section class="sec" id="vscc-examples">
    <div class="wrap">
      <div class="sec-head">
        <div class="sec-eyebrow">Worked Examples</div>
        <h2>See the logic in action</h2>
        <p class="desc">Real scenarios where KW-based and bill-based counts disagree — and why we always go with the larger one.</p>
      </div>

      <div class="tabs">
        <button class="tab-btn active" data-vscc-tab="res">🏠 Residential</button>
        <button class="tab-btn" data-vscc-tab="com">🏢 Commercial</button>
        <button class="tab-btn" data-vscc-tab="ind">🏭 Industrial</button>
      </div>

      <div class="eg-grid eg-panel active" data-vscc-panel="res">
        <div class="eg-card">
          <h4>Mr. Rohan — Mumbai</h4>
          <div class="eg-sub">Low load, high bill</div>
          <div class="eg-stats">
            <div class="eg-stat"><div class="k">Load</div><div class="v">5 KW</div></div>
            <div class="eg-stat"><div class="k">Bill</div><div class="v">₹18,000</div></div>
          </div>
          <div class="eg-result"><span class="rl">Cards Required</span><span class="rn">3 Cards</span></div>
          <p class="eg-why">5 KW only needs 1 card, but ₹18,000 needs 3 — the bill-based count wins.</p>
        </div>
        <div class="eg-card">
          <h4>Mr. Rahul Mohan</h4>
          <div class="eg-sub">Higher load, lower bill</div>
          <div class="eg-stats">
            <div class="eg-stat"><div class="k">Load</div><div class="v">15 KW</div></div>
            <div class="eg-stat"><div class="k">Bill</div><div class="v">₹10,000</div></div>
          </div>
          <div class="eg-result"><span class="rl">Cards Required</span><span class="rn">3 Cards</span></div>
          <p class="eg-why">15 KW needs 3 cards, while ₹10,000 only needs 2 — the KW-based count wins.</p>
        </div>
      </div>

      <div class="eg-grid eg-panel" data-vscc-panel="com">
        <div class="eg-card">
          <h4>Mr. Deepak Sawlani</h4>
          <div class="eg-sub">Low load, high bill</div>
          <div class="eg-stats">
            <div class="eg-stat"><div class="k">Load</div><div class="v">10 KW</div></div>
            <div class="eg-stat"><div class="k">Bill</div><div class="v">₹24,000</div></div>
          </div>
          <div class="eg-result"><span class="rl">Cards Required</span><span class="rn">4 Cards</span></div>
          <p class="eg-why">10 KW needs 2 cards, but ₹24,000 needs 4 — the bill-based count wins.</p>
        </div>
        <div class="eg-card">
          <h4>Mr. Satyajeet</h4>
          <div class="eg-sub">High load, lower bill</div>
          <div class="eg-stats">
            <div class="eg-stat"><div class="k">Load</div><div class="v">35 KW</div></div>
            <div class="eg-stat"><div class="k">Bill</div><div class="v">₹29,000</div></div>
          </div>
          <div class="eg-result"><span class="rl">Cards Required</span><span class="rn">6 Cards</span></div>
          <p class="eg-why">35 KW needs 6 cards, while ₹29,000 only needs 5 — KW-based count wins.</p>
        </div>
      </div>

      <div class="eg-grid eg-panel" data-vscc-panel="ind">
        <div class="eg-card">
          <h4>Mr. Rajeev Sharma</h4>
          <div class="eg-sub">Low load, high bill</div>
          <div class="eg-stats">
            <div class="eg-stat"><div class="k">Load</div><div class="v">90 KW</div></div>
            <div class="eg-stat"><div class="k">Bill</div><div class="v">₹2,40,000</div></div>
          </div>
          <div class="eg-result"><span class="rl">Cards Required</span><span class="rn">40 Cards</span></div>
          <p class="eg-why">90 KW needs 15 cards, but ₹2,40,000 needs 40 — bill-based count wins.</p>
        </div>
        <div class="eg-card">
          <h4>Mr. Ashish Patel</h4>
          <div class="eg-sub">High load, lower bill</div>
          <div class="eg-stats">
            <div class="eg-stat"><div class="k">Load</div><div class="v">180 KW</div></div>
            <div class="eg-stat"><div class="k">Bill</div><div class="v">₹1,20,000</div></div>
          </div>
          <div class="eg-result"><span class="rl">Cards Required</span><span class="rn">30 Cards</span></div>
          <p class="eg-why">180 KW needs 30 cards, while ₹1,20,000 only needs 20 — KW-based count wins.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- BADGES -->
  <section class="sec" style="padding-top:0;">
    <div class="wrap">
      <div class="badges-row">
        <div class="badge-item"><div class="big">20–30%</div><div class="lbl">Saved on electricity bill</div></div>
        <div class="badge-item"><div class="big">10 Yrs</div><div class="lbl">Long product life</div></div>
        <div class="badge-item"><div class="big">3 Yrs</div><div class="lbl">Replacement warranty</div></div>
        <div class="badge-item"><div class="big">100%</div><div class="lbl">Eco-friendly</div></div>
        <div class="badge-item"><div class="big">100%</div><div class="lbl">Legal to use</div></div>
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="sec" style="padding-bottom:80px;">
    <div class="wrap">
      <div class="cta-band">
        <h3>Know your number. Order the right count the first time.</h3>
        <p>Built on US–German nanotechnology with a Terahertz chip — ISO-certified and ready for home, commercial, or industrial connections.</p>
        <a href="#vscc-calc" class="btn-white">Calculate My Cards ↑</a>
      </div>
    </div>
  </section>

  <!-- STICKY MOBILE BAR (MATCHES USER PHOTO EXACTLY) -->
  <div class="vscc-sticky-bar" id="vscc-stickyBar">
    <div>
      <div class="vscc-sticky-txt">CARDS REQUIRED</div>
      <div class="vscc-sticky-num"><span id="vscc-stickyNum">1</span> <span id="vscc-stickyUnit">Card</span></div>
    </div>
    <a href="#vscc-calc" class="vscc-sticky-btn">Adjust Values ↑</a>
  </div>

</div>

<script>
(function(){
  'use strict';

  /* ---- CHART DATA ---- */
  const CHART = [
    ['1–6 KW',   'Upto ₹6,000',         1, 2],
    ['7–12 KW',  '₹6,001 – ₹12,000',    2, 3],
    ['13–18 KW', '₹12,001 – ₹18,000',   3, 4],
    ['19–24 KW', '₹18,001 – ₹24,000',   4, 5],
    ['25–30 KW', '₹24,001 – ₹30,000',   5, 6],
    ['31–36 KW', '₹30,001 – ₹36,000',   6, 7],
    ['37–42 KW', '₹36,001 – ₹42,000',   7, 8],
    ['43–48 KW', '₹42,001 – ₹48,000',   8, 9],
    ['49–54 KW', '₹48,001 – ₹54,000',   9, 10],
    ['55–60 KW', '₹55,001 – ₹60,000',  10, 11],
  ];

  /* build desktop table & mobile cards */
  var tbody = document.getElementById('vscc-chartBody');
  var mobileContainer = document.getElementById('vscc-mobileCards');

  CHART.forEach(function(row){
    if(tbody){
      var tr = document.createElement('tr');
      var billTxt = row[1].startsWith('Upto') ? row[1].replace('Upto', 'Up to') : row[1].replace(/ – /g, '–');
      tr.innerHTML =
        '<td style="text-align:left;">' + row[0] + '</td>' +
        '<td style="text-align:center;">' + billTxt + '</td>' +
        '<td style="text-align:center;">' + row[2] + '</td>' +
        '<td style="text-align:center;">' + row[3] + '</td>';
      tbody.appendChild(tr);
    }
  });

  /* ---- CALCULATOR ---- */
  var kwSlider    = document.getElementById('vscc-kwSlider');
  var kwNumber    = document.getElementById('vscc-kwNumber');
  var kwVal       = document.getElementById('vscc-kwVal');
  var billNumber  = document.getElementById('vscc-billNumber');
  var billVal     = document.getElementById('vscc-billVal');
  var btnSingle   = document.getElementById('vscc-btnSingle');
  var btnThree    = document.getElementById('vscc-btnThree');
  var resultNum   = document.getElementById('vscc-resultNum');
  var resultUnit  = document.getElementById('vscc-resultUnit');
  var kwCardsEl   = document.getElementById('vscc-kwCards');
  var billCardsEl = document.getElementById('vscc-billCards');
  var baseWinEl   = document.getElementById('vscc-baseWin');
  var phaseRow    = document.getElementById('vscc-phaseRow');
  var chipMeter   = document.getElementById('vscc-chipMeter');
  var stickyBar   = document.getElementById('vscc-stickyBar');
  var stickyNum   = document.getElementById('vscc-stickyNum');
  var stickyUnit  = document.getElementById('vscc-stickyUnit');

  var threePhase = false;

  function inr(n){ return '₹' + Number(n).toLocaleString('en-IN'); }

  function sync(){
    var kw   = Math.max(1, Number(kwNumber.value) || 1);
    var bill = Math.max(0, Number(billNumber.value) || 0);

    kwVal.textContent   = kw + ' KW';
    billVal.textContent = inr(bill);
    kwSlider.value      = Math.min(kw, 200);
    kwSlider.style.setProperty('--fill', (Math.min(kw,200)/200*100) + '%');

    var kwCards   = Math.max(1, Math.ceil(kw / 6));
    var billCards = bill > 0 ? Math.max(1, Math.ceil(bill / 6000)) : 1;
    var base      = Math.max(kwCards, billCards);
    var total     = threePhase ? base + 1 : base;

    kwCardsEl.textContent   = kwCards;
    billCardsEl.textContent = billCards;
    baseWinEl.textContent   = base;
    phaseRow.style.display  = threePhase ? 'flex' : 'none';
    resultNum.textContent   = total;
    resultUnit.textContent  = total > 1 ? 'Cards' : 'Card';
    stickyNum.textContent   = total;
    stickyUnit.textContent  = total > 1 ? 'Cards' : 'Card';

    /* Estimated Savings (20%-30%) */
    var savingsMin = Math.round((bill * 0.20) / 100) * 100;
    var savingsMax = Math.round((bill * 0.30) / 100) * 100;
    var savingsRangeEl = document.getElementById('vscc-savingsRange');
    if(savingsRangeEl){
      savingsRangeEl.textContent = inr(savingsMin) + ' – ' + inr(savingsMax);
    }

    /* dynamic mini gold cards row */
    var miniCardsRow = document.getElementById('vscc-miniCardsRow');
    if(miniCardsRow){
      miniCardsRow.innerHTML = '';
      var capped = Math.min(total, 30);
      for(var i=0; i<capped; i++){
        var icon = document.createElement('div');
        icon.className = 'vscc-mini-card-icon';
        icon.title = 'Card ' + (i + 1);
        miniCardsRow.appendChild(icon);
      }
      if(total > 30){
        var more = document.createElement('span');
        more.style.cssText = 'font-family:IBM Plex Mono,monospace;color:#f0631b;font-size:12px;font-weight:700;align-self:center;margin-left:4px;';
        more.textContent = '+' + (total - 30) + ' more';
        miniCardsRow.appendChild(more);
      }
    }
  }

  kwSlider.addEventListener('input', function(){ kwNumber.value = kwSlider.value; sync(); });
  kwNumber.addEventListener('input', sync);
  billNumber.addEventListener('input', sync);

  btnSingle.addEventListener('click', function(){
    threePhase = false;
    btnSingle.classList.add('active'); btnThree.classList.remove('active');
    sync();
  });
  btnThree.addEventListener('click', function(){
    threePhase = true;
    btnThree.classList.add('active'); btnSingle.classList.remove('active');
    sync();
  });

  /* ---- TABS ---- */
  document.querySelectorAll('.vscc .tab-btn').forEach(function(btn){
    btn.addEventListener('click', function(){
      document.querySelectorAll('.vscc .tab-btn').forEach(function(b){ b.classList.remove('active'); });
      document.querySelectorAll('.vscc .eg-panel').forEach(function(p){ p.classList.remove('active'); });
      btn.classList.add('active');
      var panel = document.querySelector('.vscc .eg-panel[data-vscc-panel="' + btn.dataset.vsccTab + '"]');
      if(panel) panel.classList.add('active');
    });
  });

  /* ---- STICKY BAR ---- */
  var calcSection = document.getElementById('vscc-calc');
  if(calcSection && stickyBar){
    var io = new IntersectionObserver(function(entries){
      entries.forEach(function(e){
        if(e.boundingClientRect.top < 0 && !e.isIntersecting){
          stickyBar.classList.add('show');
        } else if(e.isIntersecting){
          stickyBar.classList.remove('show');
        }
      });
    }, {threshold: 0});
    io.observe(calcSection);
  }

  sync();
})();
</script>
